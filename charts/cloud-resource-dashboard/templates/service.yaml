apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-resource-dashboard.fullname" . }}
  labels:
    {{- include "cloud-resource-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-resource-dashboard.selectorLabels" . | nindent 4 }}
