apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloud-resource-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "cloud-resource-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloud-resource-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
