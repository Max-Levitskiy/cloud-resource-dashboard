apiVersion: v1
kind: Service
metadata:
  name: {{ include "crd-api.fullname" . }}
  labels:
    {{- include "crd-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crd-api.selectorLabels" . | nindent 4 }}
